#include <iostream>
#include <cmath>
int main(){
    int x = 2;
    int y =3;
    int a = std :: max(x,y);
    int b = std :: min(x,y);
    
    std :: cout << a; 
   //z = std::max(x, y);
   //z = std::min(x, y);
   //z = pow(2, 4);
   //z = sqrt(9);
   //z = abs(-3);
   //z = round(x);
   //z = ceil(x);
   //z = floor(x);

}